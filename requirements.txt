# Campus AI Chatbot - Minimal Dependencies for Vercel
# Optimized for serverless deployment (under 250MB)

# Core Framework
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# PDF Processing (lightweight)
PyPDF2==3.0.1

# Utilities
python-dateutil==2.8.2

# ========== REMOVED FOR VERCEL ==========
# These are too heavy for Vercel's 250MB limit:
# - chromadb (200MB+) - Use simple JSON storage instead
# - sentence-transformers (500MB+) - Use keyword search
# - transformers (1GB+) - Using cloud APIs instead
# - spacy (500MB+) - Not needed
# - nltk (100MB+) - Not needed
# - ollama - Local only
# - eventlet/socketio - Not supported on Vercel
#
# Total package size: ~50MB ✅
# gTTS==2.4.0
